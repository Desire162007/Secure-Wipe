# Requirements - requirements.txt
# FastAPI and web server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=11.0.3
python-multipart>=0.0.6

# Device detection and system info
psutil>=5.9.6

# Optional OS-specific device detection (install per OS)
pyudev>=0.24.0; platform_system == "Linux"
WMI>=1.5.1; platform_system == "Windows"
pywin32>=306; platform_system == "Windows"

# PDF generation
reportlab>=4.0.7

# Data validation and async support  
pydantic>=2.4.2
asyncio-extra>=1.3.2

# HTTP client for testing
httpx>=0.25.0

# Development and debugging
python-dotenv>=1.0.0
rich>=13.7.0

# Optional: Enhanced logging
loguru>=0.7.2